export default function CartPage() {
  return (
    <div>
      <h1 className="text-3xl font-bold">Your Cart</h1>
      <p className="mt-2">Cart is empty (prototype).</p>

      <button className="mt-4 px-4 py-2 bg-black text-white rounded">
        Checkout
      </button>
    </div>
  );
}
